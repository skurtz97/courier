version: '3'

tasks:
  build:
    cmds:
      - docker-compose build
  start:
    cmds:
      - docker-compose up
  stop:
    cmds:
      - docker-compose down
  clean:
    cmds:
      - docker system prune